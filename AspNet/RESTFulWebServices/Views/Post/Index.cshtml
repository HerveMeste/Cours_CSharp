@model List<Post>

@{
    ViewBag.Title = "User's Posts";
    Layout = "_Layout";
}

<h1>Post Searching</h1>

<form class="my-5 text-center" method="get" src="https://localhost:44391/Post/Index">
    <div class="col-4 mb-3 text-center mx-auto">
        <label for="inputUsers" class="form-label">Choose your id's user</label>
        <input type="number" id="inputUsers" class="form-control" name="userId" required min="1" max="10" />
        <div id="numberHelp" class="form-text">(a number between 1 and 10)</div>
    </div>
    <button type="submit" class="btn btn-primary">SUBMIT</button>
</form>

@{
    if (Model.Count > 0)
    {
        <h4>There are @Model.Count post from your User choice.</h4>
        <table class="table table-bordered text-center table-responsive">
            <thead>
                <tr>
                    <th>Title</th>
                    <th>Content</th>
                </tr>
            </thead>
            <tbody>
                @foreach (Post post in Model)
                {
                    <tr>
                        <td class="align-middle">@post.title</td>
                        <td class="align-middle">@post.body</td>
                    </tr>
                }
            </tbody>
        </table>
    }
}